# Never commit secrets
wrike.env
.env
.env.*
token.txt

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# local data snapshot (never ship publicly)
data.js

# local shared-state store (server.mjs dev only)
.state.json
